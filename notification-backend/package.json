{
  "name": "bloodconnect-notification-backend",
  "version": "1.0.0",
  "description": "Small backend service that sends FCM v1 notifications for new blood requests.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --forceExit --detectOpenHandles"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  },
  "dependencies": {
    "express": "^4.19.2",
    "express-rate-limit": "^7.1.5",
    "firebase-admin": "^12.5.0",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0",
    "pino-http": "^10.4.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "supertest": "^7.2.2"
  }
}
