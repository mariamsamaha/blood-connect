// lib/screens/recipient_home_screen.dart
import 'package:flutter/material.dart';

class RecipientHomeScreen extends StatelessWidget {
  const RecipientHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recipient Home')),
      body: const Center(child: Text('Recipient view - TODO')),
    );
  }
}